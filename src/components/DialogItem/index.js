import React from 'react';
import classNames from 'classnames';

import './DialogItem.scss';

const DialogItem = ({ children, className }) => {
  return <div className="dialogs__item"></div>;
};

export default DialogItem;
